---
import { stack } from '../utils/data';
---

<section class="max-w-xl mx-auto mt-14 px-4 font-sans">
  <p class="text-2xl font-bold text-heading mb-6">Stack</p>

  {Object.entries(stack).map(([category, items]) => (
    <div class="mb-5">
      <p class="text-muted text-[11px] font-mono uppercase tracking-wider mb-3">{category}</p>
      <div class="flex flex-wrap gap-2">
        {items.map((tech) => (
          <span class="text-body text-xs px-3 py-1 rounded-full border border-border-default bg-transparent transition-all duration-300 hover:border-white hover:text-white hover:-translate-y-0.5 cursor-default">
            {tech}
          </span>
        ))}
      </div>
    </div>
  ))}
</section>
